# User Guide

1. Train the system with `python -m nlp_pipeline.main train --config configs/pipeline.yaml`
2. Start the API with `python -m nlp_pipeline.main serve`
3. Call `/predict`, `/analyze`, `/search`, or `/chat` from your client

