# Deployment Guide

- Install dependencies from `requirements.txt`
- Train the model to generate artifacts in `artifacts/`
- Run the FastAPI app with Uvicorn
- Use Docker Compose for API plus observability services

